/**
 * @pkg/ui/nav/MainTabs — v14.0 4탭 alias.
 * 데스크탑은 TopBar 하단, 모바일은 하단 고정 — 둘 다 PhonaraNav 가 처리.
 */
import PhonaraNav from "@/components/nav/PhonaraNav";

export default function MainTabs() {
  return <PhonaraNav />;
}
