/**
 * @pkg/operator — Operator Mode (LOCKED v3.0 §13-3)
 *
 * 이 패키지는 운영자 전용. user app에서 import 금지 (Week 4 ESLint rule).
 *
 * 현재는 placeholder. Week 1에 admin 컴포넌트를 점진 이전.
 */
export const OPERATOR_PARTITION = "admin" as const;
