Phonara — Edge Function secrets that MUST be present in the new Supabase project.
Set with:  supabase secrets set KEY=value  (after `supabase link --project-ref wyhhdyrvqtoejvusnhva`)

# Auto-injected by Supabase (do NOT set manually)
SUPABASE_URL                        ← injected
SUPABASE_ANON_KEY                   ← injected
SUPABASE_SERVICE_ROLE_KEY           ← injected

# Lovable AI Gateway (replace if moving off Lovable)
LOVABLE_API_KEY                     ← required by ai-bot-run, ai-mission-generator,
                                       ai-support-reply, ai-daily-ops-report,
                                       concierge-suggest, dm-composer, posting-scheduler

# Cron / internal auth
BOT_CRON_SECRET                     ← shared secret used by cron callers

# Web Push (VAPID)
VAPID_PUBLIC_KEY
VAPID_PRIVATE_KEY
VAPID_SUBJECT                       ← e.g. mailto:ops@phonara.world

# SMS (Twilio) — withdrawal OTP
TWILIO_API_KEY
TWILIO_FROM_NUMBER

# LINE webhook (KR/JP support inbox)
LINE_CHANNEL_ACCESS_TOKEN
LINE_CHANNEL_SECRET

# Liquidation engine tuning (optional; has default)
MAINTENANCE_MARGIN_RATIO_THRESHOLD  ← e.g. 0.05
